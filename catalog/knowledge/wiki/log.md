# Knowledge Base Log

> Append-only chronological record of wiki operations.
> Entry format: `## [YYYY-MM-DD] <op> | <subject>` where `<op>` is
> `ingest | query | update | lint`.
> `grep "^## \[" wiki/log.md | tail -5` shows the last five entries.

_(no operations yet — the wiki starts empty; ingest a source to begin)_
